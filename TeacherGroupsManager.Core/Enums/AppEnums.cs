namespace TeacherGroupsManager.Core.Enums;

public enum GroupType { Private = 1, Public = 2 }
public enum LessonType { Private = 1, Group = 2 }
public enum AttendanceStatus { Present = 1, Absent = 2, Excused = 3 }
public enum PaymentStatus { Unpaid = 1, PartiallyPaid = 2, Paid = 3 }
public enum AppRole { Admin = 1, Teacher = 2, AssistantTeacher = 3 }
